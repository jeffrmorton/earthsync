{
  "name": "earthsync-detector",
  "version": "1.1.10",
  "description": "EarthSync Data Detector/Simulator",
  "main": "detector.js",
  "private": true,
  "dependencies": {
    "dotenv": "^16.4.5",
    "ioredis": "^5.4.1",
    "winston": "^3.14.2"
  },
  "scripts": {
    "start": "node detector.js",
    "lint": "eslint . --ext .js",
    "lint:fix": "eslint . --ext .js --fix",
    "format": "prettier --write \"**/*.{js,json,yaml,yml}\"",
    "format:check": "prettier --check \"**/*.{js,json,yaml,yml}\"",
    "test": "echo \"Error: no test specified\" && exit 0"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "prettier": "^3.3.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
